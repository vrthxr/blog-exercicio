<div id="<%= dom_id post %>">
  <div>
    <strong>Title:</strong>
    <%= post.title %>
    <p><%=pluralize(post.views, "view")%></p>
    <p>publicado por: <%= post.user.email %></p>
  </div>

  <div>
    <strong>Body:</strong>
    <%= post.body %>
  </div>

</div>
